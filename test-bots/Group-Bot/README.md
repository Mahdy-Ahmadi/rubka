# 🛡️ راهنمای ربات مدیریتی گروه


---

## ۱️⃣ نصب و فعال‌سازی ربات ⚙️
```diff
+ نصب / فعال / مالک
```
- نصب ربات در گروه و تعیین کاربر نصب‌کننده به عنوان مالک.
- اگر گروه قبلاً ربات را داشته باشد، این دستور تاثیری ندارد.

---

## ۲️⃣ مدیریت مالک و ادمین‌ها 👑
### 🔹 تعیین مالک
- مالک گروه با اولین کاربری که ربات را نصب می‌کند تعیین می‌شود.
```diff
+ نصب / فعال / مالک
```

### ➕ افزودن ادمین
- دستور: `افزودن ادمین`
- روی پیام کاربر ریپلای کنید.

### ➖ حذف ادمین
- دستور: `حذف ادمین`
- روی پیام کاربر ریپلای کنید.

### 📋 مشاهده لیست ادمین‌ها
- دستور: `لیست ادمین`
- نمایش تمام ادمین‌های کمکی.

---

## ۳️⃣ مدیریت حالت‌های گروه 🔧
### 🚨 حالت سختگیر (Strict Mode)
```diff
+ حالت سختگیر روشن
- حالت سختگیر خاموش
```
- در حالت سختگیر، هر تخلف منجر به اخراج خودکار می‌شود.

### 🗣️ سخنگو (Speaker Mode)
```diff
+ سخنگو روشن
- سخنگو خاموش
```
- فعال/غیرفعال کردن پاسخ خودکار ربات.

---

## ۴️⃣ قوانین گروه 📜
### 🔹 مشاهده وضعیت قوانین
```diff
 وضعیت / قفل ها
```
- نمایش فعال/غیرفعال بودن قوانین.

### 🔒 خاموش / روشن کردن همه قوانین
```diff
- خاموش همه / همه خاموش
+ روشن همه / همه روشن
```

### 🎯 فعال / غیرفعال کردن قانون خاص
- مثال: `قفل لینک / قفل منشن`
- تغییر وضعیت قوانین خاص مانند لینک، منشن، هشتگ و غیره.

---

## ۵️⃣ مدیریت پیام‌ها 📨
### 🗑️ حذف پیام‌ها
```diff
 حذف <تعداد پیام>
```
- مثال: `حذف 30`
- فقط ادمین‌ها می‌توانند اجرا کنند.

### ℹ️ مشاهده اطلاعات پیام
```diff
 info / اطلاعات / get
```
- روی پیام ریپلای کنید و اجرا کنید.

---

## ۶️⃣ اخطار و جریمه ⚠️
| دستور | توضیح |
|-------|-------|
| `اخطار` | اضافه کردن اخطار به کاربر ریپلای شده. اخراج خودکار بعد از رسیدن به حد نصاب. |
| `حذف اخطار` | حذف یک اخطار از کاربر ریپلای شده. |
| `لیست اخطار` | نمایش کاربران با تعداد اخطارهای دریافتی. |
| `تعداد اخطار <عدد>` | تعیین تعداد اخطار برای اخراج خودکار. |

---

## ۷️⃣ سکوت کاربران 🤫
| دستور | توضیح |
|-------|-------|
| `سکوت <مدت زمان>` | بی‌صدا کردن کاربر برای مدت مشخص. |
| `سکوت دائمی` | بی‌صدا کردن دائم. |
| `حذف سکوت` | برداشتن سکوت کاربر ریپلای شده. |
| `پاکسازی سکوت` | حذف تمام رکوردهای سکوت. |
| `لیست سکوت` | مشاهده لیست کاربران سکوت‌شده. |

---

## ۸️⃣ مدیریت گروه 🏗️
| دستور | توضیح |
|-------|-------|
| `ری استارت گروه` | پاک کردن همه داده‌ها. بعد از آن، دستور `فعال` لازم است. |
| `انتقال اطلاعات <گروه جدید>` | انتقال اطلاعات گروه به گروه دیگر. |
| `قفل گروه <مدت زمان>` | جلوگیری از ارسال پیام. |
| `باز کردن قفل گروه` | باز کردن قفل گروه. |

---

## ۹️⃣ آمار و پروفایل کاربران 📊
| دستور | توضیح |
|-------|-------|
| `پروفایل` | نمایش پیام‌ها، اخطارها، سکوت، رتبه کاربر. |
| `آمار` | تعداد پیام‌های کاربر. |
| `آمار گروه` | گزارش آماری کامل گروه. |
| `اطلاعات گروه` | نمایش اطلاعات کامل گروه شامل مالک، وضعیت، قوانین و تنظیمات. |

---

## 🔟 تگ کاربران 🏷️
- دستور: `تگ / تگ <تعداد کاربران>`  
- تگ کردن کاربران ذخیره شده در گروه، پیش‌فرض ۲۰ نفر در هر پیام.  
- فقط ادمین‌ها می‌توانند اجرا کنند.

---

## ۱۱️⃣ قوانین سختگیر و چک خودکار 🔍
- بررسی خودکار پیام‌ها برای تخلفات: لینک، منشن، هشتگ، ایموجی، عدد، دستور، فایل، عکس، ویدیو، موزیک و غیره.
- حالت سختگیر: اخراج خودکار. حالت عادی: اخطار.

---

## ۱۲️⃣ بکاپ و پشتیبان‌گیری 💾
- دستور: `بکاپ گروه`  
- ذخیره تمام اطلاعات گروه در فایل JSON.  
- فقط مالک قادر به اجرای دستور است.

---
---
## ۱۳️⃣ پنل مدیریت خصوصی (پی‌وی ربات) 🧩

این ربات دارای یک پنل مدیریتی دکمه‌ای در پی‌وی می‌باشد که به مالک گروه‌ها اجازه می‌دهد بدون ارسال دستور در گروه، مدیریت کامل گروه‌های خود را انجام دهد.

---

### 📌 دسترسی به پنل
- دستور ورود به پنل:
```diff
/start
```
- پنل فقط در **چت خصوصی ربات** فعال است.
- فقط **مالک گروه‌ها** به این بخش دسترسی دارد.

---

### 🗂️ گروه‌های من
- با انتخاب دکمه **«گروه‌های من»** لیست تمام گروه‌هایی که شما مالک آن‌ها هستید نمایش داده می‌شود.
- برای هر گروه، دو گزینه در دسترس است:
  - 📄 **نام گروه** → نمایش اطلاعات کامل گروه
  - 🗑️ **حذف** → حذف کامل اطلاعات گروه از دیتابیس ربات

---

### ℹ️ اطلاعات گروه (پنل خصوصی)
با انتخاب نام هر گروه، اطلاعات زیر نمایش داده می‌شود:

| مورد | توضیح |
|----|------|
| 🆔 چت آیدی | شناسه یکتای گروه |
| 📡 وضعیت گروه | فعال / غیرفعال |
| 👥 اعضای ذخیره‌شده | تعداد کاربران ثبت‌شده |
| 🔇 کاربران سکوت‌شده | تعداد کاربران در حالت سکوت |
| ⚠️ کاربران اخطاردار | تعداد کاربران دارای اخطار |
| 💬 مجموع پیام‌ها | کل پیام‌های ثبت‌شده |

---

### ⚙️ تنظیمات نمایش داده‌شده
در بخش اطلاعات گروه، وضعیت تنظیمات زیر نیز قابل مشاهده است:
- سخنگو (Speaker)
- حالت سختگیر (Strict)
- سقف اخطار
- قفل گروه
- سکوت خودکار

---

### 🗑️ حذف اطلاعات گروه
- با انتخاب دکمه **«حذف گروه»**، پیام تأیید نمایش داده می‌شود.
- در صورت تأیید عملیات:
  - تمام اطلاعات گروه از دیتابیس حذف می‌شود.
  - برای فعال‌سازی مجدد، ارسال دستور `فعال` در گروه الزامی است.
- امکان لغو عملیات حذف وجود دارد.

---

### ⚠️ نکات امنیتی پنل
- فقط مالک گروه قادر به مشاهده و مدیریت اطلاعات گروه‌ها می‌باشد.
- هیچ‌یک از عملیات پنل در داخل گروه اجرا نمی‌شود.
- تمامی عملیات مدیریتی از طریق دکمه‌ها انجام می‌شود.
---
