from rubka.asynco import Robot

# اگر سرور مشکل نداره اضافه نکنید یعنی اگه تایم اوت نمیده دست نزنید به ورودی

bot = Robot("token") # این برای سرور سالم

# اما در صورتی که خطای تایم اوت دریافت میکنید 

bot = Robot("token",api_endpoint="messenger") # اپی ادپوینت روی مسنجر قرار داده شه که خطای تایم اوت از بین بره
